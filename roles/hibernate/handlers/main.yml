---
# HUP rereads logind.conf; restarting systemd-logind would end the session.
- name: Reload logind
  become: true
  ansible.builtin.command: systemctl kill -s HUP systemd-logind
  changed_when: true
